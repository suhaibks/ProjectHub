{
  "name": "projecthub",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:env && vite build",
    "preview": "vite preview",
    "test": "echo \"No tests configured\"",
    "check:env": "node scripts/validate-env.mjs",
    "validate:package": "node -e \"JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('package.json valid')\""
    "check:env": "node scripts/validate-env.mjs"
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"No tests configured\""
  },
  "dependencies": {
    "firebase": "^12.11.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-firebase-hooks": "^5.1.1",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^5.4.19"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
